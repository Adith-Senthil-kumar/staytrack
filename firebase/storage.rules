rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Each owner can only read/write files under their own namespace.
    match /users/{uid}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
